type Props = {
  params: {},
  searchParams: {
    error: string,
  },
}

const ChatPage = ({ searchParams: { error }}: Props) => {
  return (
    <div>
      {/* admin controls */}
      chat 
    </div>
  )
}

export default ChatPage